---
import BaseLayout from '../layouts/BaseLayout.astro'
import Footer from '../components/atoms/Footer.astro'
import BackTop from '../components/atoms/BackTop.astro'
import TwitterCard from '../components/cards/TwitterCard.astro'
import TelegramCard from '../components/cards/TelegramCard.astro'
import Typography from '../components/atoms/Typography.astro'
import Button from '../components/atoms/Button.astro'
---

<BaseLayout>
  <section class="flex flex-col p-20 px-16 md:flex-row">
    <div class="md:w-1/2">
      <Typography variant="h2">Contactános</Typography>
      <Typography variant="p"
        >Estamos felices de que nos quieras enviar un saludo una consulta por
        datos o algún mail en particular.
      </Typography>
      <form class="my-8 flex flex-col gap-2 md:mr-8">
        <div class="mb-4 flex flex-col gap-4 md:flex-row">
          <div class="w-full">
            <label
              class="text-gray-700 mb-2 block text-sm font-bold"
              for="username"
            >
              Nombre
            </label>
            <input
              class="text-gray-700 focus:shadow-outline w-full appearance-none rounded border px-3 py-2 leading-tight shadow focus:outline-none"
              id="username"
              type="text"
              placeholder="Nombre"
            />
          </div>
          <div class="w-full">
            <label
              class="text-gray-700 mb-2 block text-sm font-bold"
              for="lastname"
            >
              Apellido
            </label>
            <input
              class="text-gray-700 focus:shadow-outline w-full appearance-none rounded border px-3 py-2 leading-tight shadow focus:outline-none"
              id="lastname"
              type="text"
              placeholder="Apellido"
            />
          </div>
        </div>
        <label
          class="text-gray-700 mb-2 block text-sm font-bold"
          for="lastname"
        >
          Correo
        </label>
        <input
          class="text-gray-700 focus:shadow-outline w-full appearance-none rounded border px-3 py-2 leading-tight shadow focus:outline-none"
          id="email"
          type="text"
          placeholder="Correo"
        />
        <label
          class="text-gray-700 mb-2 block text-sm font-bold"
          for="lastname"
        >
          Motivo
        </label>
        <div class="relative">
          <select
            class="bg-gray-200 border-gray-200 text-gray-700 focus:border-gray-500 block w-full appearance-none rounded border px-4 py-3 pr-8 leading-tight focus:bg-white focus:outline-none"
            id="grid-state"
          >
            <option>Solicitar datos de la red</option>
            <option>Colaboración en investigación</option>
            <option>Información sobre pasantías (FIUNA)</option>
            <option>Qué hacer en caso de AQI muy altos</option>
            <option>Pedido sobre información de Salud </option>
          </select>
          <div
            class="text-gray-700 pointer-events-none absolute inset-y-0 right-0 flex items-center px-2"
          >
            <svg
              class="fill-current h-4 w-4"
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 20 20"
              ><path
                d="M9.293 12.95l.707.707L15.657 8l-1.414-1.414L10 10.828 5.757 6.586 4.343 8z"
              ></path></svg
            >
          </div>
        </div>
        <label
          class="text-gray-700 mb-2 block text-sm font-bold"
          for="lastname"
        >
          Mensaje
        </label>
        <textarea
          class="text-gray-700 focus:shadow-outline w-full appearance-none rounded border px-3 py-2 leading-tight shadow focus:outline-none"
          id="message"
          placeholder="Escribe tu mensaje..."
          rows="8"></textarea>
        <Button text="Enviar" variant="color" />
      </form>
    </div>
    <div class="mt-[1/2] flex flex-col gap-4 md:w-1/2">
      <TelegramCard />
      <TwitterCard />
    </div>
  </section>
  <BackTop />
  <Footer />
</BaseLayout>
